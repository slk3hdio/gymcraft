# GymCraft 宣传视频 · 分镜脚本（约 90 秒）

> 目标观众：强化学习 / LLM Agent 研究者、Minecraft 模组社区、AI 编程爱好者。
> 基调：极客、利落、有"魔法成真"的瞬间。中文配音（可另配英文字幕版本）。
> 核心记忆点：**"把任何 Minecraft 生物，变成一行 `env.step()` 驱动的 Agent。"**

---

## 整体节奏

| 段落 | 时间 | 作用 |
|---|---|---|
| A. 钩子 | 0:00–0:08 | 制造好奇 |
| B. 问题 | 0:08–0:20 | 痛点共鸣 |
| C. 方案 | 0:20–0:38 | 亮出 GymCraft 与架构 |
| D. 演示 1 | 0:38–0:53 | Q-learning 跳搭方块 |
| E. 演示 2 | 0:53–1:08 | LLM 挖铁全流程 |
| F. 可扩展性 | 1:08–1:18 | 注册表 / 组件化 |
| G. 收尾 CTA | 1:18–1:30 | Logo + 仓库地址 |

音乐建议：开头 8 秒静默或低音氛围 → 0:08 起进入 120 BPM 左右的电子/芯片音乐，演示段落随训练成功给一个小高潮，结尾收干净。

---

## A. 钩子（0:00–0:08）

**画面**：
- 0:00–0:04 纯黑屏，只有一行打字机效果的代码：`env.step(action)`
- 0:04–0:08 切入游戏画面：一只僵尸站在草原上一动不动（特写，浅景深），随后突然抬头、起跳，在脚下放出一个方块。

**字幕**：无

**配音**：
> "如果 Minecraft 里的生物，可以被你的代码训练——会发生什么？"

**要点**：僵尸跳起放方块的瞬间卡一个低音鼓点，画面轻微震屏。

---

## B. 问题（0:08–0:20）

**画面**：
- 0:08–0:14 快速蒙太奇：Gymnasium 官网 logo / `CartPole` 经典画面 / MuJoCo 风格仿真，每个画面 2 秒，配"唰"的转场音效。
- 0:14–0:20 画面切到 Minecraft 广袤世界航拍镜头（延时摄影感），字幕浮现：
  > "最丰富的开放世界之一，却最难接进 RL 训练管线。"

**配音**：
> "强化学习有 Gymnasium，有无数仿真器。但 Minecraft——这个最接近真实复杂度的沙盒世界——一直没有一个干净的 Gym 接口。"

**要点**：节奏快，字幕逐词浮现，强调"没有干净的接口"这个痛点。

---

## C. 方案（0:20–0:38）

**画面**：
- 0:20–0:26 Logo 动画：GymCraft 字样 + Minecraft 风格方块元素拼合；下方徽章条扫过（Minecraft 26.1 / NeoForge / Java 25 / gRPC :50051）。
- 0:26–0:32 架构图动画（对应 README 架构）逐层点亮：
  ```
  Python Agent ── gRPC :50051 ──> GymEnvService ──> Minecraft tick
  ```
  数据包沿连线流动的粒子效果。
- 0:32–0:38 左右分屏：左边 VS Code 中真实的 Python 代码（`env.reset()` / `env.step()`），右边游戏内生物同步做出动作。

**字幕（叠加）**：
> "NeoForge 模组 × gRPC × Gymnasium 风格 API"

**配音**：
> "GymCraft，一个 NeoForge 模组：把游戏里的任何生物包装成 Gymnasium 风格环境。Python 端一行 `reset`、一行 `step`，通过 gRPC 直连游戏内的 tick 循环。"

**要点**：分屏同步是本段灵魂——左边敲下 `env.step` 回车的瞬间，右边生物起跳，让观众直观感到"代码在驱动游戏"。

---

## D. 演示 1：跳搭方块 Q-learning（0:38–0:53）

**画面**：
- 0:38–0:41 训练场全景：`parkour_mob` 环境，生物站在平地，远处高台上有目标标记。
- 0:41–0:48 **训练过程加速蒙太奇**：分三个阶段快切——
  - 早期：生物原地乱跳、乱放方块（配 `episode 12` 角标）；
  - 中期：开始搭出歪歪扭扭的楼梯（`episode 87`）；
  - 后期：干脆利落地螺旋跳搭直冲高台（`episode 200`）。
- 0:48–0:53 成功登顶，画面定格，角落叠加 Q 表奖励曲线上升的小图表。

**字幕（角标）**：`纯表格 Q-learning · 无深度学习框架 · 200 episodes`

**配音**：
> "一个只用表格 Q-learning 的 Agent：自己学会组合跳跃、放置方块、移动——踩着亲手搭的方块登上目标。没有预设脚本，全是试错学出来的。"

**要点**：这是第一个"哇"时刻。三段快切的对比必须夸张，episode 角标是可信度的关键。

---

## E. 演示 2：LLM 从空手到粗铁（0:53–1:08）

**画面**：
- 0:53–0:56 `iron_mining` 训练场：一只空手僵尸，面前是树木。字幕：`环境：iron_mining · Agent：LLM`
- 0:56–1:05 连续实机画面（可 2 倍速）配左侧滚动的 LLM 思考日志（从 `--trace` 的 jsonl 提取）：
  砍树 → 打开 self 背包 2×2 合成 → 做出并放置工作台 → 合成木镐 → 挖石头 → 合成石镐 → 挖铁矿石。
- 1:05–1:08 粗铁入手特写，物品栏高亮，"叮"的成就音效。

**字幕（叠加）**：
> "砍树 → 工作台 → 木镐 → 石镐 → 粗铁，全链路自主完成"

**配音**：
> "换一个大脑——接入任意 LLM。从空手开始：砍树、开背包、合成工作台、做镐子，一路挖到粗铁。每一步决策，都来自模型的推理。"

**要点**：左侧思考日志让"是 LLM 在决策"这件事可视化，避免观众以为是脚本回放。

---

## F. 可扩展性（1:08–1:18）

**画面**：
- 1:08–1:13 三个注册表图标式动画：`17 动作组件` / `7 观测组件` / `4 内置环境`，各自弹出代表图标（move / attack / break_block / 菜单 / nearby_entities / world …）。
- 1:13–1:18 代码特写：在 `EnvFactories` 中注册一个新 `DeferredHolder`，然后镜头拉远——一个全新的自定义环境在世界里跑起来。

**字幕（叠加）**：
> "NeoForge 自定义注册表 · 动作 / 观测 / 环境全可扩展"

**配音**：
> "十七个动作组件、七种观测生成器，全部挂在 NeoForge 自定义注册表上。写几十行代码，就能定义你自己的训练环境。"

---

## G. 收尾 CTA（1:18–1:30）

**画面**：
- 1:18–1:24 回到 Logo：GymCraft 居中，下方依次浮现 —— GitHub 仓库地址、`pip install` 式命令行（`uv add gymcraft`）、四个环境徽章。
- 1:24–1:28 最后一镜：演示 1 的生物站在高台上回望镜头，镜头缓缓拉远，整个世界全景展开。
- 1:28–1:30 黑屏，一句话字幕：
  > "你的世界，就是下一个训练场。"

**配音**：
> "GymCraft，开源、免费。拿起 Environment Tool，右键一只生物——你的世界，就是下一个训练场。"

**要点**：仓库地址务必以可读字号停留 ≥4 秒；结尾金句与开头钩子呼应。

---

## 制作备注

- **实机录制**：建议用 Replay Mod 或 OBS 录制，固定机位 + 平滑运镜；所有演示镜头来自真实运行（`demos/parkour_q_learning_demo.py`、`demos/iron_mining_llm_demo.py`），不做摆拍，可信度是本片核心卖点。
- **分屏同步**（C 段）需后期对轴：以键盘回车音为对齐点。
- **字幕规范**：全程硬字幕；代码与命令用等宽字体；关键词（`reset` / `step` / gRPC / Q-learning）做高亮色。
- **配音**：中文配音 + 英文字幕即可覆盖国内外；若做英文版，只需替换配音，字幕文件互换。
- **时长控制**：如超时，优先压缩 B 段蒙太奇（每个画面 2s → 1.5s）和 F 段图标展示数量。
